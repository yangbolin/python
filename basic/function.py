#output every element in an array using python

print "Hello python";

raw_input("Press any key to close this window...");

def sort(array):
	for index in range(len(array)):
		print(array[index])

arr = [1, 2, 3, 4, 5]

sort(arr);
